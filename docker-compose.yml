services:
  postgres:
    image: postgres
    volumes:
      - ./postgres_db/init_db.sql:/docker-entrypoint-initdb.d/init_db.sql
    environment:
      POSTGRES_PASSWORD: "senha123"
    ports:
      - "5432:5432"